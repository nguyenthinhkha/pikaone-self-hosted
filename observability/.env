# Optional env for docker-compose.observability.yml (copy to .env.observability on the host).
# Grafana reads admin user/password only when grafana_data is empty (first boot).

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=12345678x@X
